// The same fixture task, done WITHOUT touching the gate's own files. The gate
// is then independent of the change, so this run opens a real pull request
// rather than a draft. (Lines starting with // are ignored by fake_upstream.py.)
{"call": {"thought": "read the module", "tool": "read_file", "args": {"path": "src/math.js"}}}
{"call": {"thought": "add multiply", "tool": "write_file", "args": {"path": "src/math.js", "content": "'use strict';\n\nfunction add(a, b) {\n  return a + b;\n}\n\nfunction subtract(a, b) {\n  return a - b;\n}\n\nfunction multiply(a, b) {\n  return a * b;\n}\n\nmodule.exports = { add, subtract, multiply };\n"}}}
{"call": {"thought": "check the gate still passes", "tool": "run_verify", "args": {"step": "test"}}}
{"call": {"thought": "done", "tool": "finish", "args": {"summary": "added multiply", "pr_title": "Add multiply() to src/math.js", "pr_body": "Adds `multiply(a, b)`. The existing test file is untouched."}}}
