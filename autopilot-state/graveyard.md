# Graveyard

Killed hypotheses. Do not re-propose these unless the `Reason` column is "timing" — and only then with an explicit note about what's changed.

Format:

```markdown
## <hypothesis id/title>

- **Killed on:** <tick N, date>
- **Ran for:** <N ticks>
- **Reason:** no-traction | doesn't-work | timing | guardrail-violation | build-broke
- **What we learned:** <one sentence>
```

---

*(empty — first kill will seed this)*
